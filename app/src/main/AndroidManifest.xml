<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sohan.attendance">

    <application
        android:allowBackup="true"
        android:theme="@style/Theme.AfmAttendance"
        android:label="AttenX"
        android:icon="@mipmap/ic_launcher">

        <activity
            android:name=".ui.main.MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity android:name=".ui.setup.SetupActivity" />
        <activity android:name=".ui.dashboard.DashboardActivity" />
        <activity android:name=".ui.schedule.ScheduleActivity" />
        <activity android:name=".ui.graph.GraphActivity" />

    </application>

</manifest>